<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- ColorScheme.fromSeed(Brand.seed).surface, computed not guessed. Light
         is the app's default theme, so this is the colour of the first frame;
         anything else shows a card of the wrong colour for a beat. -->
    <color name="launch_background">#FFF8F6</color>
</resources>
